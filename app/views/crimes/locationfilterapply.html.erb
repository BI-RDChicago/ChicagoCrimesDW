<<%= link_to 'Back', url_for(:back) %>

<h2>Location Analisys</h2>
<p><b>Filters: </b></p>
<p>Community: <%= @communityarea_desc %></p>
<p>Start Date: <%= @start %></p>
<p>Finish Date: <%= @finish %></p>
<p>Arrest: <%= @arrest %></p>

  <div>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Date</th>
            <th>Quantity</th>
          </tr>
        </thead>            
        <tbody>
         <% @location_filtered.each do |comm| %>
         <tr>
            <td><%= comm.name %></td>
            <td><%= comm.qnt %></td>
         </tr>
         <%  end %>

        </tbody>
      </table>
  </div>